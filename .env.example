# 這個教學專案預設不需要任何金鑰。
# 預約資料只會寫到 data/appointments.json。
# 確認信只會產生本機 HTML 預覽，不會真的寄出。

# ---- 後台密碼（公開部署時必填）----
# /admin、預約狀態修改與確認信預覽都含有客戶個資，
# 沒有設定 ADMIN_PASSWORD 時這些路徑會直接停用（回應 503）。
# 本機測試：把這段複製到 .env.local 並改成自己的密碼。
ADMIN_USER=admin
ADMIN_PASSWORD=
